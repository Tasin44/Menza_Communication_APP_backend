# ── Core ─────────────────────────────────────
Django>=4.2,<5.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3     # JWT auth + blacklist for logout
mysqlclient>=2.2                        # MySQL driver

# ── Password hashing ─────────────────────────
django[argon2]                          # Argon2id — spec requirement (section D)

# ── CORS ─────────────────────────────────────
django-cors-headers>=4.3

# ── Storage (S3/Cloudflare R2) ───────────────
django-storages>=1.14
boto3>=1.34

# ── Environment variables ─────────────────────
python-decouple>=3.8                    # .env file management

# ── Dev / testing ────────────────────────────
pytest-django>=4.8
factory-boy>=3.3

# ── WebSockets ───────────────────────────────
channels>=4.0.0
daphne>=4.0.0
channels-redis>=4.0.0